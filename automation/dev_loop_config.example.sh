################################################################################
# SynergyAI 自动化开发循环配置示例
#
# 使用方法：
# 1. 复制此文件为 dev_loop_config.sh
#    cp dev_loop_config.example.sh dev_loop_config.sh
# 2. 根据需要修改配置
# 3. 在 run_dev_loop.sh 中引入此配置
################################################################################

# =============================================================================
# 服务器配置
# =============================================================================

# 开发服务器端口
SERVER_PORT=8000

# 服务器启动超时时间（秒）
SERVER_START_TIMEOUT=30

# =============================================================================
# Claude Code 配置
# =============================================================================

# Claude Code CLI 命令路径
# 如果 claude 不在 PATH 中，使用完整路径
# CLAUDE_CMD="/usr/local/bin/claude"
CLAUDE_CMD="claude"

# -----------------------------------------------------------------------------
# 初始 Prompt（每次循环都会发送给 Claude）
# -----------------------------------------------------------------------------
# 你可以根据项目需要自定义这个 Prompt

# 示例 1: 通用开发
INITIAL_PROMPT="请继续 SynergyAI 项目的开发工作。请从待办任务列表中选择一个优先级最高的任务来处理。遵循增量工作原则，一次只处理一个任务，完成后记得测试和提交。"

# 示例 2: 专注于特定模块
# INITIAL_PROMPT="请继续开发用户认证模块。当前需要实现的功能包括：1) 用户登录 2) 密码重置 3) 会话管理。请选择其中一个任务完成，并确保安全性测试通过。"

# 示例 3: Bug 修复模式
# INITIAL_PROMPT="请查看项目中的待办任务，优先处理标记为 'bug' 或 '高优先级' 的任务。修复后请进行完整测试。"

# 示例 4: 测试驱动
# INITIAL_PROMPT="请选择一个需要测试的功能，编写并运行完整的测试用例。确保覆盖正常场景、边界条件和异常情况。"

# 示例 5: 文档完善
# INITIAL_PROMPT="请检查项目文档，补充缺失的部分。包括 API 文档、使用说明、部署指南等。"

# =============================================================================
# Claude Code 参数
# =============================================================================

# 权限相关
CLAUDE_YES_FLAG="--yes"                    # 自动确认所有提示
CLAUDE_INTERACTIVE_FLAG="--no-interactive" # 非交互模式
CLAUDE_PERMS_FLAG="--allow-permissions"     # 允许所有权限

# 日志相关
CLAUDE_LOG_DIR="${PROJECT_DIR}/logs"
CLAUDE_LOG_LEVEL="info"  # debug, info, warn, error

# =============================================================================
# 循环控制
# =============================================================================

# 每轮之间的等待时间（秒）
WAIT_TIME_BETWEEN_ROUNDS=5

# 是否在失败后继续（true/false）
CONTINUE_ON_FAILURE=true

# 最大连续失败次数（超过则停止）
MAX_CONSECUTIVE_FAILURES=3

# =============================================================================
# Git 配置
# =============================================================================

# 提交前是否自动推送
AUTO_PUSH=true

# 提交信息模板
COMMIT_MSG_TEMPLATE="feat: 开发迭代 #{ITERATION} - 自动化开发流程

- 由 Claude Code 自动完成的功能开发
- 遵循增量工作原则
- 包含测试和验证

Commit: {TIMESTAMP}"

# 推送失败时是否继续
CONTINUE_ON_PUSH_FAILURE=true

# =============================================================================
# 日志配置
# =============================================================================

# 日志保留天数（0 = 永久保留）
LOG_RETENTION_DAYS=7

# 是否压缩旧日志
COMPRESS_OLD_LOGS=true

# 日志级别（DEBUG, INFO, WARN, ERROR）
LOG_LEVEL="INFO"

# =============================================================================
# 通知配置（可选）
# =============================================================================

# 完成后发送通知（需要额外配置）
# NOTIFY_ON_COMPLETE=false
# NOTIFY_EMAIL="your-email@example.com"
# NOTIFY_WEBHOOK="https://hooks.slack.com/..."

# =============================================================================
# 高级配置
# =============================================================================

# 每轮开始前执行的自定义命令
PRE_RUN_COMMAND=""

# 每轮结束后执行的自定义命令
POST_RUN_COMMAND=""

# 是否生成详细的性能报告
GENERATE_PERFORMANCE_REPORT=true

# 是否在每轮后重启服务器
RESTART_SERVER_EACH_ROUND=false

# =============================================================================
# 开发场景预设
# =============================================================================

# 取消注释你想要的场景，修改 INITIAL_PROMPT

# 场景 1: 新功能开发
# SCENARIO="new-feature"
# INITIAL_PROMPT="请实现 {FEATURE_NAME} 功能。需求如下：{REQUIREMENTS}。请按照 TDD 方式开发：先写测试，再实现功能。"

# 场景 2: Bug 修复
# SCENARIO="bug-fix"
# INITIAL_PROMPT="请修复 {BUG_DESCRIPTION}。步骤：1) 分析问题 2) 定位代码 3) 修复问题 4) 编写测试 5) 验证修复。"

# 场景 3: 代码重构
# SCENARIO="refactor"
# INITIAL_PROMPT="请重构 {MODULE_NAME} 模块。目标：1) 提高可读性 2) 优化性能 3) 添加单元测试。确保所有现有测试通过。"

# 场景 4: 文档完善
# SCENARIO="documentation"
# INITIAL_PROMPT="请完善项目文档。重点：1) API 文档 2) 使用示例 3) 部署指南 4) 故障排除。"

# 场景 5: 测试增强
# SCENARIO="testing"
# INITIAL_PROMPT="请为 {MODULE_NAME} 模块添加全面的单元测试和集成测试。确保覆盖率达到 80% 以上。"

# =============================================================================
# 使用示例
# =============================================================================

# 在 run_dev_loop.sh 中引入此配置：
# source "${PROJECT_DIR}/dev_loop_config.sh"

# 或者在运行前导出特定变量：
# export INITIAL_PROMPT="专注于性能优化..."
# ./run_dev_loop.sh 5

################################################################################
